spring:
  application:
    name: stash
  mongodb:
    uri: #secret manager

  jackson:
    time-zone: Asia/Seoul

  lifecycle:
    timeout-per-shutdown-phase: 30s

server:
  port: 9090
  shutdown: graceful

logging:
  level:
    root: INFO
    com.ravit.stash: DEBUG
